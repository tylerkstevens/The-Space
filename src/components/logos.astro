---
// @ts-ignore
import { Icon } from "astro-icon/components";

// List of sponsor images, their alt text, and URLs
const sponsors = [
  { src: "/assets/sponsors/fedimint.png", alt: "Fedimint", url: "https://fedimint.org" },
  { src: "/assets/sponsors/compassmining_logo.jpeg", alt: "Compass", url: "https://compassmining.io" },
  { src: "/assets/sponsors/bitcoin.svg", alt: "Sponsor Us", format: "svg" },
  { src: "/assets/sponsors/bitcoin.svg", alt: "Sponsor Us", format: "svg" },
  { src: "/assets/sponsors/bitcoin.svg", alt: "Sponsor Us", format: "svg" },
  { src: "/assets/sponsors/bitcoin.svg", alt: "Sponsor Us", format: "svg" },
  // Add more sponsors as needed
];
---

<style>
  .custom-gradient {
    background: linear-gradient(135deg, #f3e18a, #febe00, #E74C3C, #D9314A, #15a393);
  }
  .white-svg {
    filter: invert(100%) sepia(0%) saturate(0%) hue-rotate(0deg) brightness(100%) contrast(100%);
  }
</style>

<div class="mt-24 mb-0 custom-gradient py-12">
  <h2 class="text-center text-slate-100">Supported by bitcoin organizations and believers. Help us spread the word.</h2>
  <div class="flex gap-8 md:gap-20 items-center justify-center mt-10 mb-10 flex-wrap">
    {sponsors.map(sponsor => (
      <a href={sponsor.url} target="_blank" rel="noopener noreferrer" class="block">
        {sponsor.format === 'svg' ? (
          <div class="h-12 w-12 md:h-20 md:w-20 white-svg">
            <img src={sponsor.src} alt={sponsor.alt} class="object-contain h-full w-full" />
          </div>
        ) : (
          <img src={sponsor.src} alt={sponsor.alt} class="h-12 w-12 md:h-20 md:w-20 object-contain" />
        )}
      </a>
    ))}
  </div>
</div>